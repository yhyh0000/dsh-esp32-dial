# Codex Pocket Companion 完整需求文档

版本：0.2
状态：可进入开发排期
目标硬件：Waveshare ESP32-S3-Touch-LCD-1.85B
产品定位：带照片屏保的个人信息终端，提供语音记录、邮箱阅读和 Codex 额度看板

> ESP32 负责“看得见、摸得着、听得到”的交互；服务器负责录音存储、邮件接入、语音处理和 Sub2API 查询。敏感凭证不进入固件。

## 0. 方案结论

第一版采用以下主线：

```text
Waveshare 1.85B 官方 BSP
    + 360x360 LVGL 圆屏 UI
    + Wi-Fi / WebSocket 服务器连接
    + 语音录音上传与邮件投递
    + QQ / Gmail 邮件摘要
    + Sub2API Codex 额度适配器
```

第一版不做完整 Codex 任务控制、审批闭环和服务器管理面板。Codex 只保留额度、重置时间和服务状态；服务器面板、多人说话人实名库和更复杂的 AI 整理放到第二版。

## 1. 产品定义

### 1.1 一句话

用户平时看到预设照片；唤醒后左右滑动浏览语音记录、邮箱和 Codex 额度，录音结束后由服务器保存并发送到指定邮箱。

### 1.2 目标用户

- 需要快速记录想法、会议内容或待办的人。
- 需要在小屏上快速查看邮件和 AI 服务状态的人。
- 希望把敏感数据留在自己服务器上的个人用户。

### 1.3 核心使用场景

| 场景 | 用户动作 | 设备反馈 |
|---|---|---|
| 闲置 | 不操作设备 | 显示用户预设照片 |
| 语音记录 | 主动触发录音 | 显示录音时长、上传和发送状态 |
| 邮箱阅读 | 左右滑到邮箱页 | 显示 QQ/Gmail 未读、主题、发件人和摘要 |
| Codex 额度 | 左右滑到额度页 | 显示 Sub2API 返回的剩余量和重置时间 |
| 网络中断 | 观察设备 | 明确显示离线，录音进入待上传队列 |

## 2. 目标与非目标

### 2.1 本项目目标

1. 让设备在 360x360 圆屏上清楚表达 Codex 的实时状态。
2. 让用户能在设备上完成低风险、低复杂度的交互，例如查看、选择、确认和取消提醒。
3. 让设备可以通过局域网连接电脑，不把 Codex 密钥放入固件。
4. 让固件、Bridge、协议和网页烧录流程都可以独立二次开发。
5. 对断线、延迟、过期数据和后端异常做诚实表达。

### 2.2 明确不做的事

- 不在 ESP32-S3 上运行完整 Codex 或本地大语言模型。
- 不把设备做成完整代码编辑器、终端或文件管理器。
- 第一版不做公网开放服务，不把 Bridge 直接暴露到互联网。
- 第一版不做持续监听麦克风，录音必须由用户主动触发。
- 不假设板子有机械旋钮；1.85B 的业务输入是圆屏触摸和 BOOT 键。
- 不把 1.85C/C-BOX 项目的固件直接刷入 1.85B。

## 3. 硬件基线与约束

硬件规格以 [Waveshare ESP32-S3-Touch-LCD-1.85B 官方文档](https://docs.waveshare.com/ESP32-S3-Touch-LCD-1.85B) 和 [官方示例仓库](https://github.com/waveshareteam/ESP32-S3-Touch-LCD-1.85B) 为准。

| 项目 | 产品约束 | 主要用途 |
|---|---|---|
| 主控 | ESP32-S3，双核 LX7，最高 240 MHz | UI、网络、传感器任务 |
| 内存 | 512 KB SRAM、8 MB PSRAM、16 MB Flash | LVGL、图片、网络缓存 |
| 屏幕 | 1.85 英寸圆屏，ST77916，360x360，QSPI | Codex 状态和桌面 UI |
| 触摸 | CST816S 电容触摸 | 点击、滑动和确认 |
| 无线 | 2.4 GHz Wi-Fi、Bluetooth LE | 局域网 Bridge、后续 BLE |
| 音频 | 板载麦克风和音频编解码链路 | 提示音、后续语音输入/输出 |
| 运动 | QMI8658 六轴 IMU | 后续手势和姿态交互 |
| 电源 | 电池接口、充电和电量检测 | 电量显示、低电量策略 |
| 存储 | microSD | 后续日志、音频和资源 |
| 物理键 | BOOT 业务键；PWR 负责供电 | 背光/唤醒和刷机 |

### 3.1 UI 硬件约束

- 所有关键内容必须适配圆形裁切区域，不能依赖矩形屏的四角。
- 核心状态必须在一次抬眼查看中完成，不要求用户先进入菜单。
- 默认使用 3 行以内的活动摘要，每行控制长度，长文本由 Bridge 预先截断。
- 设备不能依赖中文字体完整覆盖所有 Codex 输出；动态内容优先使用短文本和 ASCII 阶段名。
- 不能把触摸、音频、IMU 和网络初始化分散在多个互相冲突的 BSP 中。
- 屏幕、触摸、音频和引脚定义统一从官方 1.85B BSP 读取；旧文档中与官方 BSP 不一致的音频型号或引脚描述不作为实现依据。

## 4. 系统架构

```text
                  局域网 WebSocket
┌─────────────────────────────────────────────┐
│ ESP32-S3-Touch-LCD-1.85B                    │
│  ESP-Brookesia + LVGL                       │
│  Desktop / Codex App / Settings             │
│  Touch / BOOT / Display / Audio / Battery   │
└──────────────────┬──────────────────────────┘
                   │ JSON text frames
                   ▼
┌─────────────────────────────────────────────┐
│ Node.js Codex Bridge                        │
│  Device WebSocket server                    │
│  Token authentication                        │
│  State derivation and stale detection        │
│  Optional voice and notification adapter     │
└──────────────────┬──────────────────────────┘
                   │ local stdio JSON-RPC
                   ▼
┌─────────────────────────────────────────────┐
│ codex app-server                             │
│  thread/list                                 │
│  thread/turns/list                           │
│  event / approval adapter                    │
└─────────────────────────────────────────────┘
```

### 4.1 ESP32 固件职责

- 启动显示、触摸、Wi-Fi、NVS 和 Bridge WebSocket。
- 渲染状态，不在设备端重新推断 Codex 业务状态。
- 维护连接、心跳、重连和数据新鲜度。
- 上报设备身份、电量、充电状态和后续 IMU 事件。
- 处理用户触摸、审批选择、背光和本地提示音。

### 4.2 Bridge 职责

- 在电脑本地启动并管理 `codex app-server --stdio`。
- 读取 Codex 线程和最近 turn，转换成适合圆屏的紧凑状态。
- 监听需要实时表达的事件，例如工具调用、文本流和审批请求。
- 对设备连接进行 token 认证、广播、重放和心跳管理。
- 负责文本截断、阶段判定、统计换算和错误归一化。
- 后续承载语音识别、TTS、桌面通知等电脑端能力。

### 4.3 Codex 适配原则

Codex 的内部接口可能随版本变化，固件不能直接依赖 Codex 的内部 JSON。所有变动都必须收敛在 `bridge/codex-backend.js` 和状态转换层，设备只认项目自己的协议。

## 5. 复用现有工程

当前工作区的产品工程为 `work/dsh-esp32-dial`，已经具备以下基础：

| 现有资产 | 当前路径 | 处理方式 |
|---|---|---|
| ESP-IDF + ESP-Brookesia 桌面 | `firmware/main/main.cpp` | 保留，作为系统壳层 |
| Codex Phone App | `firmware/main/app_codex/` | 保留，按本需求继续收敛 UI |
| 小蓝桌面组件 | `firmware/main/app_xiaolan/` | 保留为可选桌面组件 |
| Node Bridge | `bridge/bridge.js` | 保留，拆分 Codex 适配和设备协议 |
| Codex app-server 适配 | `bridge/codex-backend.js` | 保留，补齐事件和审批能力 |
| WebSocket/JSON 规范 | `docs/协议规范.md` | 作为协议基础，本文定义产品要求 |
| 网页烧录 | `pages_download/`、`WEB_FLASH.md` | 保留，接入正式 CI 产物 |

### 5.1 必须先整改的问题

1. 固件当前存在默认远程地址/Token 的写法，正式版必须移除，改为首次配网或本地 NVS 配置。
2. 当前 Bridge 文档默认使用 `3082`，Codex App 源码中仍有 `7002` 和远程地址痕迹，必须统一为可配置端口，开发默认 `3082`。
3. 当前 `cmd` 路径对部分停止/暂停操作仍只是记录日志，只有后端确认提供稳定接口后才能在 UI 宣称“已执行”。
4. 当前 `answer` 主要沿用 DSH 事件的应答结构；Codex app-server 的审批事件和应答必须单独适配并做集成测试。
5. `ctx` 表示上下文占用比例，不等于 ChatGPT/Codex 订阅额度，UI 文案必须保持 `CTX` 或“上下文”，不能写成“额度”。
6. 旧 Arduino 固件和旧 DSH API 只能保留为兼容参考，不再作为产品主线。

## 6. 功能需求

优先级定义：P0 为首个可用版本必须完成，P1 为可用性增强，P2 为后续扩展。

### 6.1 启动与桌面

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-001 | P0 | 上电后显示桌面，不出现黑屏、花屏或越界文字 | 连续冷启动 20 次无崩溃 |
| FR-002 | P0 | 桌面提供 Codex 入口，点击进入全屏 Codex App | 进入耗时不超过 2 秒 |
| FR-003 | P0 | Codex App 全屏显示，不显示与内容冲突的底部导航栏 | 返回桌面后网络连接不重复创建 |
| FR-004 | P1 | 桌面状态栏显示时间、Wi-Fi 和电量 | 状态每秒或事件变化时更新 |
| FR-005 | P1 | 小蓝为可选桌面组件，不遮挡 Codex 入口和状态栏 | 进入 Codex App 时自动隐藏 |

### 6.2 配网与连接

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-010 | P0 | 首次启动允许配置 Wi-Fi、Bridge 主机、端口和 Token | 配置保存后重启仍有效 |
| FR-011 | P0 | 设备通过 `ws://host:port/dev?token=...` 连接 Bridge | Token 错误时被拒绝 |
| FR-012 | P0 | WebSocket 断线自动重连，采用指数退避，最大 30 秒 | Bridge 恢复后自动恢复状态 |
| FR-013 | P0 | 设备显示连接中、在线、数据过期、离线四类状态 | 不以过期数据冒充实时数据 |
| FR-014 | P1 | Bridge `/status` 提供本机诊断 JSON | 浏览器可查看 Bridge、Codex 和设备状态 |

### 6.3 Codex 状态

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-020 | P0 | 显示 `idle`、`thinking`、`working`、`streaming`、`waiting`、`done`、`error`、`offline` | 每种状态都有可重复测试帧 |
| FR-021 | P0 | 显示当前动作摘要，例如文件、命令、工具或“输出中” | 长文本不溢出，最多显示 3 行活动 |
| FR-022 | P0 | 显示上下文占用 `CTX 0-100%` | 无数据时显示空或 `--`，不显示假零值 |
| FR-023 | P0 | 最多显示 6 个 Agent/线程槽位 | 顺序稳定，缺少的槽位显示 IDLE |
| FR-024 | P0 | 点击 Agent 槽位查看阶段和当前动作 | 选中态清楚，不改变其他 Agent 状态 |
| FR-025 | P1 | 显示任务轮次、步骤、耗时等统计 | 缺少字段时留空，不显示 NaN |
| FR-026 | P1 | 任务完成后显示完成态并播放提示音 | 完成态保持约 3 秒后回到空闲 |
| FR-027 | P1 | 任务失败显示错误态和错误提示音 | 断线不显示为完成 |

### 6.4 审批与问题应答

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-030 | P0 | 收到审批/问题时显示弹层、标题、正文和最多 3 个选项 | 设备进入 WAITING，不能被普通状态帧立即覆盖 |
| FR-031 | P0 | 触摸选项后发送 `answer`，并等待 Bridge 确认 | 重复点击不会产生重复应答 |
| FR-032 | P0 | 设备重连后 Bridge 重放仍未处理的 ask | 不因短暂断线丢失审批 |
| FR-033 | P1 | 审批超时或已在电脑端处理后关闭弹层 | 显示“已处理”或回到当前状态 |
| FR-034 | P1 | 高风险操作使用明显的拒绝/取消样式 | 不能把默认按钮误设计成危险操作 |

### 6.5 控制、音频和传感器

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-040 | P1 | 支持停止、暂停、继续等控制命令，但仅在后端能力确认后开放 | 后端无接口时按钮隐藏或标记不可用 |
| FR-041 | P1 | 任务完成、审批、错误支持本地提示音 | 无网络时仍可播放本地音效 |
| FR-042 | P2 | 用户主动触发录音，音频上传到电脑端做 ASR | 未触发时麦克风不录音 |
| FR-043 | P2 | Bridge 将文本交给 TTS，设备播放语音 | 播放期间 UI 仍能显示连接状态 |
| FR-044 | P2 | IMU 支持摇一摇取消或翻转静音 | 误触发率经过实机测试后再开放 |

### 6.6 电源与维护

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-050 | P0 | 电量和充电状态可在状态栏或 Codex 页显示 | 无电量数据时显示 `--` |
| FR-051 | P0 | BOOT 短按控制背光/唤醒，不承担不可逆业务操作 | 不影响刷机进入下载模式 |
| FR-052 | P1 | 低电量时降低背光、暂停非必要轮询 | 不丢失关键审批状态 |
| FR-053 | P1 | Bridge 生成或加载随机设备 Token | Token 文件权限尽可能收紧 |
| FR-054 | P1 | 固件显示版本、板型和 Bridge 连接信息 | 便于现场排障 |

### 6.7 第一版核心功能

本节覆盖当前产品范围；前面关于多 Agent、Codex 审批和完整桌面壳层的条目暂列为后续能力，不作为本版发布门槛。

| ID | 优先级 | 需求 | 验收 |
|---|---|---|---|
| FR-060 | P0 | 无操作达到设定时间后进入照片屏保 | 点击或滑动唤醒，返回上次页面 |
| FR-061 | P0 | 语音、邮箱、Codex 额度三个页面左右滑动切换 | 左右滑动超过阈值只切换一次，页面有位置指示 |
| FR-062 | P0 | 用户主动开始和结束录音 | 未触发时麦克风不录音，录音态有清晰反馈 |
| FR-063 | P0 | 录音上传服务器并发送到指定邮箱 | 显示上传中、发送中、已发送、失败和待重试 |
| FR-064 | P0 | 服务器保存原始录音和元数据 | 每条记录有设备 ID、开始时间、时长和唯一 ID |
| FR-065 | P0 | 邮箱页支持 QQ 和 Gmail 的未读邮件查看 | 显示账号、未读数、发件人、主题和时间 |
| FR-066 | P1 | 邮件正文可由服务器生成短摘要 | 摘要超长时服务端截断，设备不出现越界文本 |
| FR-067 | P0 | 邮箱页支持打开邮件详情 | 支持返回列表，默认不提供回复、删除和转发 |
| FR-068 | P0 | 额度页显示服务器查询的 Sub2API Codex 数据 | 显示 5 小时/7 天窗口剩余量、各自重置时间、可用重置卡数量、更新时间和可用状态 |
| FR-069 | P1 | 录音服务器处理链预留转写、时间戳和说话人字段 | 原始音频和后续处理结果通过同一记录关联 |
| FR-070 | P1 | 服务器推送数据失败时设备使用缓存并标注过期 | 不用过期数据冒充实时状态 |

## 7. UI 需求

### 7.1 视觉基线

| Token | 值 | 用途 |
|---|---|---|
| 背景 | `#0E100F` | 模拟器和设备外壳背景 |
| 主文字 | `#F4F3EC` | 标题和核心数值 |
| 次文字 | `#A1AAA1` | 辅助信息、时间和说明 |
| 薄荷绿 | `#C4ED72` | 在线、可用、准备完成 |
| 青蓝 | `#82D8D1` | Codex 额度主视觉 |
| 琥珀橙 | `#FFB968` | 邮件、等待、过期提醒 |
| 珊瑚红 | `#FF7D70` | 录音中、失败和错误 |

设计原则：暗色中使用多色语义，颜色只表达状态，不用颜色替代文字；业务页不使用复杂背景动画；设备上的文字和状态必须同时落在圆形安全区内。

#### 7.1.1 调研后的布局结论

- 官方板卡资料确认屏幕为 360x360 圆形屏幕，因此不能按矩形屏幕贴边排版；第一版内容区使用约 48-50 px 的顶部/左右安全边距，底部只保留 3 个分页点。
- 设备高频任务是“看一眼状态、按一下录音、快速扫邮件”，所以每个业务页只保留一个主任务：录音页突出主按钮，邮箱页突出未读列表，额度页突出剩余百分比。
- ESP-ADF、ESP-SR 和邮件/Sub2API 的调研结果都指向同一个架构：设备负责采集、展示和轻量交互，服务器负责录音存储、邮件接入、摘要处理和额度查询；设备 UI 只消费统一状态帧。
- 状态栏居中显示在线、时间和电量；页头显示页面名称与连接标签。这样既减少圆屏两侧裁切，也避免用户只靠颜色猜状态。
- 页面底色按任务区分：语音使用偏绿黑、邮箱使用暖黑、额度使用青黑；外壳仍保持石墨黑，形成清晰但克制的层次。

### 7.2 页面信息架构

```text
闲置照片屏保
└── 唤醒后横向页面
    ├── P0 语音记录
    │   ├── 开始/结束录音
    │   ├── 上传/邮件发送状态
    │   └── 最近一条记录
    ├── P1 邮箱
    │   ├── QQ / Gmail 账号切换
    │   ├── 未读列表
    │   └── 邮件摘要详情
    └── P2 Codex 额度
        ├── 5 小时窗口剩余百分比和重置倒计时
        ├── 7 天窗口剩余百分比和重置时间
        ├── 可用重置卡数量和最近过期时间
        └── Sub2API 更新时间和连接状态
```

照片屏保是全屏闲置层，不计入三个业务页面。左右滑动只在业务页面之间循环；邮件详情属于邮箱页内的临时子视图。

### 7.3 第一版页面 UI

#### 7.3.1 闲置照片屏保

- 无触摸、按键或业务事件达到设定时长后显示预设照片。
- 照片使用 `cover` 裁切填满 360x360 圆形屏幕，默认不显示业务卡片。
- 照片上方居中显示大号 `HH:mm` 和低对比度日期，满足桌面端抬眼看时间的需求；底部保留照片名称和唤醒提示。
- 顶部状态栏保留低对比度在线、电量信息；亮度随闲置时长降低，时间不能盖住照片主体。
- 模拟器支持调整时间的 Y 轴位置、颜色和字重；固件实现时将这些参数作为本地配置保存。
- 点击或左右滑动唤醒，返回进入屏保前的业务页面。
- 照片来源第一版支持服务器下发后缓存，后续可增加 SD 卡导入和轮播。

#### 7.3.2 语音记录页 P0

```text
┌──────────────────────────────┐
│ 语音记录                 在线 │
│                              │
│          ●                   │
│        开始录音              │
│          00:00               │
│                              │
│ 结束后上传并发送到指定邮箱    │
│ 最近：今天 15:20 · 03:42  已发送 │
└──────────────────────────────┘
```

- 点击录音主按钮开始，再次点击结束；录音期间按钮使用独立的录音态。
- 结束后依次显示本地保存、上传中、发送中、已发送、失败和待重试。
- 第一版邮件附件发送原始录音；记录模型预留转写、时间戳和说话人字段。
- 网络中断时写入本地队列，恢复连接后自动续传，不能丢失已完成录音。

#### 7.3.3 邮箱页 P1

```text
┌──────────────────────────────┐
│ 邮箱                    已同步 │
│  [QQ 邮箱]  [Google]          │
│  3 封未读              刚刚同步 │
│  ● 项目群通知       15:18     │
│    本周交付时间确认            │
│  ● 服务器告警       14:50     │
│    API 服务恢复通知            │
│  ○ 财务部             昨天     │
└──────────────────────────────┘
```

- QQ 和 Gmail 账号切换使用页内分段控件，不新增顶层页面。
- 默认显示未读和最近邮件；列表可上下滚动，横向手势仍用于切页。
- 点击邮件进入临时详情页，显示发件人、主题、时间、正文预览和服务器摘要。
- 第一版只读，不在设备上提供回复、删除、转发等不可逆操作。

#### 7.3.4 Codex 额度页 P2

```text
┌──────────────────────────────┐
│ Codex 额度          Sub2API 在线 │
│                              │
│             52%              │  5 小时窗口剩余
│          01:33:44            │  5 小时后重置
│                              │
│ 7天窗口 87%  ━━━━━  重置卡 3张 │
│ 6天15时后重置  最早9月21日过期 │
│ Plus             更新21:31 可用 │
└──────────────────────────────┘
```

- Sub2API 的 `utilization` 是已用百分比，设备展示的剩余百分比必须计算为 `100 - utilization`；示例响应中的 5 小时和 7 天窗口分别显示为 52% 和 87%。
- 额度、两个窗口的重置时间、`rate_limit_reset_credits.available_count`、最近一张卡的过期时间、可用状态和更新时间均来自服务器标准化后的状态帧。
- `rate_limit.allowed=false` 或 `limit_reached=true` 时显示受限状态；数据过期时明确显示过期或离线，不把旧额度伪装成实时数据。
- 额度低于配置阈值时改变颜色并播放一次提醒音，不能连续打扰；重置卡数量为 0 时显示“暂无可用重置卡”。

#### 7.3.5 横向切页规则

- 业务页顺序固定为：语音记录 → 邮箱 → Codex 额度。
- 向左进入下一页，向右进入上一页，首尾循环。
- 水平位移超过 48 px 且明显大于垂直位移时切页；一次手势只切一页。
- 页面底部显示 3 个位置点；详情视图打开时暂时不改变业务页位置。
- 短按、录音按钮和邮件列表点击不能触发横向切页。

### 7.4 二版 Codex 状态页（暂缓）

固定布局建议：

```text
┌──────────────────────────────┐
│ CODEX          CONNECTED     │  标题/连接
│            WORKING           │  当前阶段
│       修改 codex_app.cpp     │  当前摘要
│           ( CTX 34% )        │  中央上下文环
│                              │
│      A1   A2   A3             │  Agent 槽位
│      A4   A5   A6             │
│                              │
│ A1 WORKING                    │  当前选中
│ > 读取 app-server 状态        │  活动行 1
│ - 更新状态帧                  │  活动行 2
│                              │
│             87%  12:43        │  电量/时间
└──────────────────────────────┘
```

要求：

- 顶部显示产品名和连接态，连接态不能只靠颜色表达。
- 阶段名使用稳定短文本：`IDLE`、`THINKING`、`WORKING`、`STREAMING`、`WAITING`、`DONE`、`ERROR`、`OFFLINE`。
- 中央环表示上下文占用，不显示成订阅额度环。
- Agent 槽位显示 `A1-A6` 和阶段，选中槽位使用更粗边框或高亮。
- 活动区只显示最新且最有意义的 3 行。
- 动态文字必须换行或截断，不能越过圆形安全区。

### 7.5 二版状态表现（暂缓）

| 状态 | 主色 | 视觉 | 语音/声音 |
|---|---|---|---|
| IDLE | 灰 | 静止，显示时钟和统计 | 无 |
| THINKING | 黄 | 中央环轻微呼吸 | 可选短提示 |
| WORKING | 绿 | 活动行高亮，环轻微动效 | 无 |
| STREAMING | 绿 | 输出行更新 | 无 |
| WAITING | 黄 | 黄闪，Ask 弹层置顶 | 等待提示音 |
| DONE | 蓝 | 完成标识 | 完成提示音 |
| ERROR | 红 | 错误标识和简短原因 | 错误提示音 |
| OFFLINE | 橙 | 隐藏过期数据或降低对比度 | 无，避免重复打扰 |

### 7.6 二版 Ask 弹层（暂缓）

- 弹层覆盖总览页，但保留连接态。
- 标题最多 1 行，正文最多 4 行，超出由 Bridge 摘要或截断。
- 选项最多 3 个，从上到下排列。
- 允许类操作使用主色，拒绝/取消使用红色，普通选项使用中性色。
- 点击一次后立即禁用按钮，直到收到应答结果或超时。
- 断线期间不允许显示“已确认”，只能显示“发送中”或“离线未发送”。

### 7.7 设置与服务器绑定页

第一版只保留必要设置：

- Wi-Fi SSID 和密码。
- Bridge 主机名/IP、端口和 Token。
- 亮度、音量和提示音开关。
- Bridge 连通性测试。
- 固件版本、板型和最近一次错误。

配置写入 NVS。密码和 Token 不显示完整明文，不进入普通日志。

## 8. Bridge 与设备协议

### 8.1 连接

开发默认端点：

```text
ws://<server-host>:3082/dev?token=<32 hex chars>
```

产品版本不得硬编码远程 IP、Token、邮箱凭据或 Sub2API 凭据。所有默认值只能用于本地开发，并且必须可以被配置覆盖。生产环境使用 `wss://` 或通过 VPN 暴露服务。

### 8.2 下行 `state`

```json
{
  "t": "state",
  "phase": "working",
  "title": "Codex",
  "detail": "修改 codex_app.cpp",
  "ctx": 34,
  "turns": 12,
  "steps": 48,
  "sessions": 4,
  "running": 1,
  "agents": [
    {"id":"thread-1","label":"A1","phase":"working","running":true,"detail":"WORKING"}
  ],
  "acts": [
    {"t":"读取 app-server 状态","r":true},
    {"t":"更新状态帧","r":false}
  ],
  "clock": "12:43",
  "seq": 1234
}
```

字段要求：

- `phase` 是 Bridge 归一化后的业务状态，固件不重复推断。
- `ctx` 为上下文占用百分比，范围 0-100；无数据时使用 0 仍必须通过状态或字段存在性区分真实零值。
- `agents` 最多 6 项，固件忽略多余项。
- `acts` 最多 3 项，`r=true` 表示当前正在进行。
- `seq` 用于丢弃旧帧，旧帧不能覆盖新状态。
- `clock` 来自电脑端或经过同步的设备时间，不能在离线时伪造实时 Codex 时间。

### 8.3 下行 `ask`

```json
{
  "t": "ask",
  "id": "ask-7f3e9a",
  "kind": "approval",
  "title": "执行命令？",
  "body": "删除构建目录",
  "options": [
    {"id":"allow","label":"允许","style":"primary"},
    {"id":"deny","label":"拒绝","style":"danger"}
  ],
  "expiresAt": 1786840300000
}
```

### 8.4 上行消息

```json
{"t":"hello","fw":"codex-pocket/0.1.0","board":"ESP32-S3-Touch-LCD-1.85B"}
{"t":"answer","id":"ask-7f3e9a","choice":"allow"}
{"t":"ping","battery":87,"charging":false}
{"t":"cmd","action":"stop"}
```

### 8.5 提示音消息

```json
{"t":"beep","kind":"done"}
{"t":"beep","kind":"waiting"}
{"t":"beep","kind":"error"}
```

### 8.6 数据新鲜度

| 状态 | 判定 | UI 规则 |
|---|---|---|
| loading | 已连接，尚未收到首帧 | 显示加载/等待 |
| live | 最近一帧在 3 秒内 | 正常显示 |
| stale | 超过 3 秒未收到状态 | 数值降对比度并显示过期 |
| offline | WebSocket 已断开 | 不显示会误导用户的运行数据 |

### 8.7 第一版服务器数据帧

服务器统一把外部服务转换为设备能直接渲染的短字段，设备不直接访问邮箱、Sub2API 或 AI 服务。

```json
{
  "t": "companion",
  "photo": {"enabled": true, "name": "预设照片", "url": "/media/photo/selected"},
  "recording": {"state": "sent", "durationSec": 222, "id": "REC-20260908-1520"},
  "mail": {"provider": "qq", "unread": 3, "updatedAt": 1788846000000},
  "quota": {
    "provider": "sub2api",
    "plan": "plus",
    "windows": {
      "fiveHour": {"usedPercent": 48, "remainingPercent": 52, "resetAt": 1788879945000, "remainingSeconds": 5624},
      "sevenDay": {"usedPercent": 13, "remainingPercent": 87, "resetAt": 1789448745000, "remainingSeconds": 574424}
    },
    "resetCards": {"availableCount": 3, "nextExpiresAt": 1789984320026},
    "status": "available",
    "updatedAt": 1788874319103
  }
}
```

允许的 `recording.state`：`idle`、`recording`、`queued`、`uploading`、`sending`、`sent`、`failed`。

允许的 `quota.status`：`available`、`low`、`exhausted`、`unknown`、`offline`。`unknown` 和 `offline` 不能渲染为正常额度。

## 9. Bridge 内部需求

### 9.1 Codex / Sub2API 适配

第一版不读取 Codex 线程和审批，只从服务器现有 Sub2API 接口获取 Codex 额度。Sub2API 的接口路径和字段可能随版本变化，必须封装在服务器适配器，不能写入固件。

服务器必须完成：

1. 定时查询额度并记录查询时间。
2. 把 Sub2API 的 `utilization` 转换为 `remainingPercent = 100 - utilization`，并把 5 小时窗口、7 天窗口和 `rate_limit_reset_credits` 转换为统一字段。
3. 请求失败时保留上一次数据但标记 `stale` 或 `offline`。
4. 不把 Sub2API 管理密钥下发到设备。
5. 通过 fixture 覆盖可用、低额度、耗尽、接口错误和字段缺失。

二版如需完整 Codex 任务状态，再恢复 `codex app-server --stdio` 适配器，并保持在服务器端。

历史 Codex 任务适配器要求如下，当前版本暂缓：

1. 线程列表排序和最多 20 个线程读取。
2. 最近 turn 的阶段转换。
3. `commandExecution`、`fileChange`、`mcpToolCall`、`dynamicToolCall`、`reasoning` 等活动转换。
4. `inProgress` 且无具体活动时显示 THINKING。
5. 有工具执行时显示 WORKING。
6. 文本增量到达时显示 STREAMING。
7. turn completed 后生成短暂 DONE 边沿状态。
8. failed/error 转为 ERROR。
9. app-server 退出转为 OFFLINE，并通知设备。

### 9.2 邮箱适配

服务器实现两个邮箱连接器：

- QQ：IMAP + 授权码，轮询未读和最近邮件。
- Gmail：OAuth + Gmail API，第一版可以轮询，稳定后再启用 push/watch。

统一输出发件人、主题、时间、未读状态、正文预览和可选摘要。凭据只保存在服务器，设备只接收脱敏后的展示数据。

### 9.3 录音与邮件投递

1. 设备为每次录音生成唯一 ID，并分片上传服务器。
2. 服务器先保存原始音频和元数据，再进入发送队列。
3. 使用配置好的发件账号把音频作为附件发送到指定邮箱。
4. 发送成功后保留记录；失败时指数退避并推送 `failed` 或 `queued`。
5. 为后续转写、时间戳和说话人识别预留处理状态和结果字段。

### 9.4 审批适配（第二版）

审批不能只做 UI 假动作。Bridge 必须保存：

- ask ID。
- 来源线程和 turn。
- Codex 后端应答所需的 request ID、审批 ID 或问题 ID。
- 选项 ID 到后端选项值的映射。
- 创建时间、过期时间和处理结果。

未完成 Codex app-server 的真实审批闭环前，设备上的按钮只能使用自测假数据，不得在正式构建中宣称可审批。

### 9.5 诊断接口

`GET /status` 至少返回：

```json
{
  "bridge": "online",
  "backend": "sub2api",
  "codexOnline": true,
  "mail": {"qq": "online", "gmail": "online"},
  "recordingQueue": 0,
  "deviceCount": 1,
  "port": 3082,
  "lastStateAt": 1786840217529,
  "lastError": null
}
```

## 10. 开发步骤与里程碑

### M0：冻结需求和环境

产出：

- 确认目标板确实为 1.85B。
- 确认采用 ESP-IDF + ESP-Brookesia。
- 删除或隔离固件中的默认远程地址和 Token。
- 统一 Bridge 端口为可配置，开发默认 3082。
- 确认 GitHub 仓库、Actions 和 Pages 的发布权限。

完成标准：源码中不再存在正式版不可接受的硬编码凭据。

### M1：硬件冒烟

顺序：

1. 用官方 1.85B BSP 启动屏幕。
2. 验证 360x360 显示方向和圆形裁切。
3. 验证 CST816S 点击和滑动。
4. 验证 Wi-Fi 连接和重连。
5. 验证 BOOT 背光/唤醒。
6. 验证电量读取、充电状态和 NVS。
7. 最后再验证音频和 IMU。

完成标准：硬件驱动问题与 Codex 业务问题分离，串口日志能明确指出失败模块。

### M2：服务器适配器单机可运行

顺序：

1. 启动服务器 API 和 WebSocket 服务。
2. 接入 Sub2API 额度查询并固定内部字段。
3. 接入 QQ IMAP 和 Gmail OAuth，只读取邮件元数据和正文预览。
4. 实现录音上传、存储、邮件发送和失败重试队列。
5. 打开 `/status` 检查邮箱、Sub2API 和录音队列状态。
6. 用固定 JSON fixture 测试可用、过期、错误和字段缺失。

完成标准：没有 ESP32 时，也能通过 API 验证三类服务的标准化返回值。

### M3：设备协议联调

顺序：

1. 固件保存服务器配置并连接 WebSocket。
2. 设备发送 `hello`。
3. 服务器立即下发当前 `companion` 状态。
4. 固件每 10 秒发送 `ping`。
5. 服务器回复 `pong`。
6. 断开 Bridge，验证 offline/stale。
7. 恢复服务器，验证自动重连和状态恢复。

完成标准：设备不需要重新刷机即可修改服务器地址、端口和 Token。

### M4：第一版 UI

实现顺序：

1. 闲置照片屏保和唤醒。
2. 三个业务页左右滑动和位置指示。
3. 录音页开始、结束和发送状态。
4. 邮箱页 QQ/Gmail 切换、列表和详情。
5. Codex 额度环、重置倒计时和过期状态。
6. 连接态、电量、时钟和版本信息。

完成标准：使用 fixture 可以逐项截图验收，任何动态文字都不越界。

### M5：录音上传与邮件投递

1. 设备生成录音 ID 并上传分片。
2. 服务器保存原始音频、开始时间和时长。
3. 服务器把附件发送到指定邮箱。
4. 验证失败重试、断网续传和重复提交幂等性。
5. 验证邮件主题、正文元数据和附件名称。

完成标准：一段录音可以从设备到服务器再到收件箱完整闭环。

### M6：邮箱阅读

1. QQ 邮箱授权码接入和未读同步。
2. Gmail OAuth 接入和未读同步。
3. 统一邮件列表和详情数据模型。
4. 验证切换账号、正文截断、过期缓存和断线恢复。

完成标准：板子可以查看两种邮箱的最新邮件摘要，且不暴露凭据。

### M7：Sub2API Codex 额度

1. 服务器定时查询 Sub2API 当前额度。
2. 统一剩余量、重置时间、账号和状态字段。
3. 验证额度低、耗尽、未知、接口错误和数据过期。
4. 服务器推送额度给设备并在页面上显示更新时间。
5. 增加低额度提醒和通知去重。

完成标准：Sub2API 发生额度变化或查询失败时，设备能显示对应状态，不伪造实时数据。

语音转写、时间戳对齐、讲话人识别和完整 Codex 控制列入二版；第一版只保留录音元数据字段和服务器处理接口。

### M8：GitHub 构建和网页烧录

推荐发布链：

```text
本地修改
  -> GitHub Pull Request / main
  -> GitHub Actions: ESP-IDF build
  -> 生成 merged firmware + manifest + SHA256
  -> GitHub Pages 更新烧录页面
  -> WebSerial 选择 USB 串口并烧录
```

必须发布：

- 固件合并镜像。
- 分区表和必要的 bootloader 镜像。
- 构建 commit、版本号和时间。
- SHA256 校验值。
- 回滚到上一稳定构建的链接。
- 网页烧录失败时的串口排障提示。

## 11. 测试与验收

### 11.1 功能测试

| 类别 | 用例 |
|---|---|
| 启动 | 冷启动、热重启、低电量启动、无 Wi-Fi 启动 |
| 显示 | 所有阶段、长标题、空数据、中文和特殊字符 |
| 触摸 | Agent 选择、Ask 选择、误触、快速重复点击 |
| 网络 | 首次连接、错误 Token、Bridge 重启、Wi-Fi 断开 |
| Codex | 单线程、多线程、工具调用、流式输出、失败和完成 |
| 审批 | 允许、拒绝、选项、超时、重连后重放 |
| 电源 | 背光、充电、低电量、睡眠唤醒 |
| 发布 | Actions 构建、产物下载、网页串口烧录、回滚 |

### 11.2 性能目标

- Bridge 状态变化到设备显示：目标不超过 2 秒。
- Bridge 正常运行时状态轮询：默认 1 秒，避免无变化时高频广播。
- 设备 WebSocket 断线后自动恢复：网络恢复后 30 秒内。
- UI 主循环不能因 JSON 解析、音频或网络阻塞。
- 长时间运行 8 小时不发生内存持续增长、UI 卡死或重复连接。

### 11.3 安全要求

- Bridge 默认只监听局域网所需接口，公网使用必须显式配置。
- 每台设备使用随机 Token，Token 不写入 Git、不显示在普通状态页。
- 录音必须用户主动触发，并在 UI 上有明确录音状态。
- 审批操作必须显示具体动作和选项，不能用“确认”代替具体含义。
- 日志中不打印 Wi-Fi 密码、Token、API Key、完整音频和敏感会话内容。
- 所有第三方源码保留原许可证和版权声明。

## 12. 源码复用策略与许可证

| 来源 | 角色 | 使用策略 |
|---|---|---|
| [当前产品工程](https://github.com/suhmyh/dsh-esp32-dial) | 产品主工程 | 继续演进，新增代码按当前仓库许可证 |
| [Waveshare 1.85B 官方仓库](https://github.com/waveshareteam/ESP32-S3-Touch-LCD-1.85B) | 精确硬件 BSP/示例 | 保留 Apache-2.0 和版权声明 |
| [78/xiaozhi-esp32](https://github.com/78/xiaozhi-esp32) | 后续语音 AI 参考/候选基座 | MIT；先拆模块验证，不与 P0 桌面壳层强行合并 |
| [xinnan-tech/xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server) | 后续自建 AI 服务端 | MIT；作为语音和 MCP 后端候选 |
| [p4ku/1.85C-AI](https://github.com/p4ku/ESP32-S3-Touch-LCD-1.85C-AI) | UI/语音/SD 参考 | Apache-2.0；只移植模块，不能直接刷写 |
| [d4rkmen/flatsphere](https://github.com/d4rkmen/flatsphere) | 本地 TTS/时钟/音频参考 | Apache-2.0；先核对具体硬件变体 |
| [Codex-Macro32](https://github.com/streetlightstartupnotes/Codex-Macro32) | 交互启发和协议参考 | 必须遵守其分层许可；不直接复制其新增 UI、品牌或受限代码 |

许可证原则：MIT/Apache-2.0 也不是“可以不管来源”。复制代码时保留版权和许可证；没有明确许可证的仓库只作为阅读参考，不能直接纳入产品发布包。

## 13. 风险与决策门

| 风险 | 影响 | 决策 |
|---|---|---|
| Codex app-server 接口变化 | Bridge 失效 | 所有 Codex 变化只改后端适配层，并保留 fixture |
| 审批事件没有稳定公开入口 | 无法实现真实审批 | 在 M6 前确认接口，否则先发布只读状态版 |
| 1.85B 与 1.85C 混用驱动 | 黑屏、触摸错位、音频异常 | 每个项目记录板型和许可证，禁止直接刷 C 固件 |
| 圆屏文字过长 | 信息不可读 | Bridge 预格式化，固件只渲染短字段 |
| Wi-Fi 断线导致假状态 | 错误决策 | 强制 live/stale/offline 四态 |
| 音频上传链路不稳定 | 录音丢失或重复投递 | 设备本地队列、服务器幂等 ID 和失败重试 |
| 网络端口和 Token 混乱 | 无法连接或泄露 | 默认 3082、全部可配置、发布前扫描硬编码 |

### 13.1 第一版发布门槛

只有同时满足以下条件，才称为“可烧录体验版”：

- 1.85B 实机冷启动稳定。
- 闲置照片屏保、唤醒和三页左右滑动稳定。
- 录音能上传服务器并发送到指定邮箱。
- QQ/Gmail 邮箱能显示未读和邮件详情。
- Sub2API 额度能正确显示可用、低额、耗尽和离线状态。
- 服务器不含默认远程 Token、邮箱凭据和 Sub2API 密钥。
- GitHub Actions 能重复构建。
- 网页烧录能写入正确镜像并显示版本。
- 网络中断时录音不丢失，恢复后能自动重试。

## 14. 最终交付物

### 固件

- ESP-IDF 工程源码。
- 1.85B 固件构建配置。
- Desktop、Codex App、设置和设备协议实现。
- 版本化资源和字体说明。

### 服务器端

- 设备 WebSocket/API 服务。
- 录音存储、发送队列和邮件投递服务。
- QQ IMAP、Gmail OAuth 邮箱适配器。
- Sub2API Codex 额度适配器。
- 统一状态协议、fixture 和自测脚本。
- Docker Compose、配置、启动、停止和日志说明。

### 发布与使用

- GitHub Actions 构建工作流。
- 固件 artifact、manifest 和 SHA256。
- GitHub Pages 网页烧录页面。
- 首次配网、Bridge 配置和故障排查文档。
- 实机验收报告。

## 15. 开工顺序

实际开发按下面顺序执行：

1. 先处理硬编码地址、Token、邮箱凭据和端口冲突。
2. 以官方 BSP 验证 1.85B 屏幕、触摸、Wi-Fi 和电量。
3. 固化 `companion/recording/mail/quota/ping` 协议和 fixture。
4. 完成照片屏保、三页横向 UI 和离线/过期状态。
5. 完成录音上传、服务器存储和指定邮箱投递。
6. 接入 QQ/Gmail 只读邮件和详情摘要。
7. 接入 Sub2API 额度查询、缓存和低额度提醒。
8. 最后接 GitHub Actions、网页烧录和版本回滚。

第一阶段的明确产物是：**一块闲置时显示预设照片、可以录音并投递邮件、可以查看 QQ/Gmail 邮件和 Sub2API Codex 额度，并可通过 GitHub 构建和网页烧录的 1.85B 实体终端。**
