# DSH ↔ ESP32 协议规范

本文档是桥接进程与固件之间的契约，以及桥接进程读取 DSH 的方式。所有字段都来自对运行中的 DSH v0.1.0-rc.6 的实测，不是猜测。

---

## 第一部分：DSH 侧（桥接进程如何读 DSH）

### 1.1 传输

DSH 的 Web API 是 **Typert RPC**，两条通道：

| 通道 | 用途 | 地址 |
|------|------|------|
| **HTTP POST** | 单次调用（查询状态） | `http://127.0.0.1:3080/api/<method>` |
| **WebSocket** | 事件推送（会话变化、审批请求） | `ws://127.0.0.1:3080/api/events.mux` |

**信任围栏**：DSH 只接受 `Host` 为环回地址、且 `Origin` 缺失或与 Host 匹配的请求。Node 的 `http.request` 默认不发 `Origin`，所以桥接进程直连即可，无需任何绕过。这与浏览器必须经过网关的原因相同。

### 1.2 请求 wire 格式

```jsonc
// POST /api/session.list
{
  "type": "client-request",
  "rpcId": "<uuid v4>",        // 每次调用新生成
  "method": "session.list",    // 必须与 URL 路径一致
  "payload": {}                // 方法参数
}
```

响应：

```jsonc
{
  "type": "server-response",
  "rpcId": "<回传同一个 uuid>",
  "result": {
    "ok": true,
    "value": { /* 方法返回值 */ }
  }
}
// 或
{
  "type": "server-response",
  "rpcId": "...",
  "result": {
    "ok": false,
    "error": { "code": "bad-request", "message": "...", "details": {...} }
  }
}
```

**踩坑记录**：`method` 只写在 URL 里不够，body 里也必须有，且 `type: "client-request"` 不可省。缺任何一个都返回 `bad-request: invalid client-request message`。

### 1.3 可用端点（实测）

| 端点 | 状态 | 返回 |
|------|------|------|
| `session.list` | ✅ 可用 | 会话列表（**仪表盘的主数据源**） |
| `settings.describe` | ✅ 可用 | `{ writable, hasDocument, namespaces[11] }` |
| `subagent.list` | ⚠️ 需要参数 | `bad-request: invalid payload` |
| `respond` | ⚠️ 需要参数 | 审批/提问的**应答**入口 |
| `session.get` / `jobs.list` / `goals.list` / `approvals.list` | ❌ 404 | 不存在 |

**关键结论**：没有 `approvals.list`，但有 `respond` —— 说明**审批是通过 WebSocket 推送的，不能轮询**。仪表盘必须常连 WebSocket 才能捕获"等你回答"这个最重要的状态。

### 1.4 `session.list` 返回结构（完整实测）

```jsonc
{
  "items": [
    {
      "sessionId": "session-031d6e5f-6ac3-4176-a896-f9c7b8eff5d4",
      "updatedAt": 1786840217529,          // 毫秒时间戳
      "running": true,                      // ★ 是否正在跑
      "blank": false,                       // 空会话（没发过消息）
      "cwd": "D:\\deepseek Harness\\工作区1",
      "agentPreset": "standard",
      "projections": {
        "asOfSeq": 226066,                  // 事件序号，可用于判断新鲜度
        "values": {
          "title": "询问 AI 助手身份",        // ★ 会话标题
          "sessionStats": {
            "turns": 85,                    // ★ 对话轮数
            "steps": 784,                   // ★ 步骤数（进度感）
            "llmMs": 27608679,              // LLM 累计耗时
            "toolMs": 14833737,             // 工具累计耗时
            "ttftMs": 22265723,
            "ttftSteps": 757,
            "decodeMs": 5342956,
            "decodeTokens": 442240
          },
          "tokenUsage": {
            "uncachedInputTokens": 47658531,
            "outputTokens": 442240,
            "cacheReadTokens": 49008844,
            "cacheWriteTokens": 0
          },
          "contextPressure": {              // ★★ 上下文占用（仪表盘外环）
            "pressureTokens": 203315,
            "projectedTokens": 203362,
            "contextWindow": 262144         // 203315/262144 = 78%
          },
          "contextBreakdown": {
            "systemTokens": 1553,
            "toolsTokens": 6670,
            "messageTokens": 113472
          },
          "permissions": {                  // ★ 当前权限档
            "options": [
              { "value": "read-only", "name": "read-only" },
              { "value": "workspace-write", "name": "workspace-write" },
              { "value": "danger-full-access", "name": "danger-full-access" }
            ],
            "currentValue": "workspace-write"
          },
          "goal": null,                     // ★ 长任务目标（有则显示）
          "todos": null,                    // ★ 任务清单
          "plan": { "active": false, "pending": false },
          "subagent": null,                 // ★ 子智能体状态
          "subagentTiming": { "settledMs": 0 },
          "sessionListMetadata": {
            "blank": false,
            "lastPromptAt": 1786840217529
          },
          "imageLimits": { /* 图片上限，仪表盘不需要 */ }
        }
      }
    }
    // ... 更多会话
  ]
}
```

**仪表盘用到的字段**（★ 标记）：`running`、`title`、`turns`、`steps`、`contextPressure`、`permissions.currentValue`、`goal`、`todos`、`subagent`。

### 1.5 WebSocket 事件帧（实测确认）

连接 `ws://127.0.0.1:3080/api/events.mux` 后自动订阅活跃会话，无需发送订阅消息。所有帧的外层格式：

```jsonc
{
  "type": "server-request",
  "rpcId": "7d665fc3-56b4-41b9-bc99-460abe2fa4a5",
  "method": "session/jobs",          // ★ 帧类型在 method 里
  "payload": { /* 见下 */ }
}
```

**四种实测帧类型**：

#### `session/subscribed` — 连接建立确认

```jsonc
{
  "type": "session/subscribed",
  "sessionId": "session-031d6e5f-6ac3-4176-a896-f9c7b8eff5d4",
  "lastSeq": 238500
}
```

#### `session/jobs` — ★ 后台任务列表（仪表盘 `detail` 的最佳来源）

```jsonc
{
  "type": "session/jobs",
  "sessionId": "session-031d6e5f-...",
  "jobs": [
    {
      "id": "pwsh-1",
      "kind": "pwsh",
      "label": "$env:BRIDGE_PORT='3099'\nnode bridge.js",   // 完整命令，含换行
      "status": "running",
      "startedAt": 1786841035124
    }
  ]
}
```

> **桥接处理**：取第一个 `status === "running"` 的 job，`label` 按行拆分、跳过 `$env:` 赋值行，取第一行有效命令截断到 40 字符 —— 这就是圆屏上"它正在干什么"那一行。

#### `session/event` — 会话事件流（高频，每个 token 一帧）

```jsonc
{
  "type": "session/event",
  "sessionId": "session-031d6e5f-...",
  "event": {
    "type": "assistant/chunk",      // 或 tool/call、assistant/message
    "seq": 241049,
    "time": 1786841274338,
    "data": {
      "turn": 85,
      "step": 58,
      "chunk": {
        "type": "reasoning-delta",   // block-start / reasoning-delta /
                                     // tool-call-delta / block-end / usage / finish
        "index": 0,
        "text": "Let"                // 流式增量
      }
    }
  }
}
```

已观察到的 `event.type`：`assistant/chunk`、`assistant/message`、`tool/call`  
已观察到的 `chunk.type`：`block-start`、`reasoning-delta`、`tool-call-delta`、`block-end`、`usage`、`finish`

> **桥接处理**：**故意不用**。这个流每个 token 一帧，转发到 ESP32 会淹没串口和 WiFi，且圆屏没有渲染流式文本的空间。仪表盘用 `session/jobs` 的粗粒度状态就够了。

#### `session/projection` — 投影更新推送

与 `session.list` 里的 `projections.values` 同结构，实时推送。桥接用它免去部分轮询。

### 1.6 审批帧 — 尚未捕获

在 20-30 秒的抓包窗口内**没有观察到**审批/提问帧，因为期间没有触发审批。桥接的实现方式是**宽容匹配**：

```javascript
if (/approv|permission|user-question|userQuestion|ask/i.test(method)) { ... }
```

并在命中时把完整 payload 打进日志（`ASK-SHAPED FRAME: ...`）。这样第一次真实审批发生时，日志会记录准确结构，届时可据此收紧匹配。

**已确认的相关事实**：
- `approvals.list` / `approvals.pending` 等端点**不存在**（404）
- `respond` 端点**存在**（返回 `bad-request` 而非 404，说明需要正确参数）

因此审批必然是推送式的，常连 WebSocket 是唯一途径。

---

## 第二部分：桥接 ↔ 固件（自定义协议）

### 2.1 传输选择

| | 选择 | 理由 |
|---|---|---|
| 传输 | **WebSocket over WiFi** | 双向、实时、ESP32 库成熟；不用 BLE 是因为 Windows 的 BLE 栈麻烦（参考项目只支持 macOS 就是这个原因） |
| 角色 | **桥接 = 服务端，固件 = 客户端** | 板子上电主动连，重连逻辑在固件侧，桥接无需发现设备 |
| 端口 | **默认 3082** | 避开 3080（DSH）和 3081（远程网关） |
| 编码 | **JSON 文本帧** | ESP32 用 ArduinoJson 解析；二进制省的那点带宽不值得增加调试难度 |

### 2.2 认证

固件连接时在 URL 携带预共享 token：

```
ws://<pc-ip>:3082/dev?token=<32 hex chars>
```

token 由桥接首次启动生成，存 `$DSH_HOME/esp32-bridge/device-token.txt`，你把它填进固件配置。**局域网内也要认证** —— 否则同网段任何设备都能读你的会话标题、发审批应答。

### 2.3 桥接 → 固件（下行）

#### `state` — 仪表盘状态（核心帧，1Hz 或变化时推送）

```jsonc
{
  "t": "state",
  "phase": "idle" | "working" | "waiting" | "done" | "error",
  "title": "询问 AI 助手身份",     // 会话标题，UTF-8，固件截断显示
  "detail": "编译 web 包",         // 当前动作（可空）
  "ctx": 78,                       // 上下文占用百分比 0-100（外环）
  "turns": 85,
  "steps": 784,
  "perm": "workspace-write",       // 权限档，决定颜色警示
  "sessions": 4,                   // 会话总数
  "running": 1,                    // 正在跑的会话数
  "agents": [                      // Codex App 的最多 6 个会话圆钮
    { "id": "thread-id", "label": "A1", "phase": "thinking", "running": true, "detail": "THINKING" }
  ],
  "seq": 227080                    // 事件序号，固件用于丢弃过期帧
}
```

`agents` 是 Codex backend 的扩展字段，旧 DSH App 会忽略它。Codex App 只显示前六项，
使用 `phase` 选择圆钮颜色，并允许点击圆钮查看该 Agent 的当前阶段；`detail` 采用短 ASCII
文本以避免设备字体缺少中文字形时出现方块。

`phase` 的判定规则（桥接侧计算，固件不做业务判断）：

| phase | 条件 | 圆屏表现 |
|-------|------|---------|
| `idle` | 无 running 会话，无待答 | 暗色 + 时钟 |
| `working` | 有 running 会话 | 蓝色呼吸环 + detail |
| `waiting` | 有待答审批/提问 | **黄闪 + 大字 + 按钮 + 提示音** |
| `done` | 刚从 running 变为非 running | 绿 ✓ + 完成音（3 秒后回 idle） |
| `error` | 会话报错 / 桥接与 DSH 断连 | 红 ✕ + 错误音 |

#### `ask` — 需要你决策（触发 waiting）

```jsonc
{
  "t": "ask",
  "id": "ask-7f3e9a",              // 应答时回传
  "kind": "approval" | "question",
  "title": "执行命令？",
  "body": "Remove-Item -Recurse D:\\build",   // 固件圆屏截断显示
  "options": [                      // 最多 3 个，固件渲染为按钮
    { "id": "allow", "label": "允许", "style": "primary" },
    { "id": "deny",  "label": "拒绝", "style": "danger" }
  ],
  "expiresAt": 1786840300000        // 超时后固件自动回 idle
}
```

#### `speak` — 语音播报（可选，v1.5）

```jsonc
{ "t": "speak", "text": "任务已完成" }
```

#### `beep` — 提示音

```jsonc
{ "t": "beep", "kind": "done" | "waiting" | "error" }
```

#### `pong` — 心跳应答

```jsonc
{ "t": "pong", "at": 1786840217529 }
```

### 2.4 固件 → 桥接（上行）

#### `hello` — 连接后自报身份

```jsonc
{
  "t": "hello",
  "fw": "dsh-dial/0.5.0",
  "board": "ESP32-S3-Touch-LCD-1.85B",
  "battery": 87,
  "charging": false
}
```

#### `answer` — 应答审批/提问（★ 设备存在的理由）

```jsonc
{
  "t": "answer",
  "id": "ask-7f3e9a",              // 回传 ask 的 id
  "choice": "allow"                // options[].id 之一
}
```

#### `cmd` — 控制命令

```jsonc
{ "t": "cmd", "action": "stop" }   // stop | pause | resume
```

#### `ping` — 心跳（固件每 10s 发一次）

```jsonc
{ "t": "ping", "battery": 87, "charging": false }
```

#### `sensor` — 传感器上报（可选，30s 一次）

```jsonc
{
  "t": "sensor",
  "battery": 87,
  "charging": false,
  "imu": { "shake": false }        // 摇一摇手势（v1.5）
}
```

### 2.5 连接生命周期

```
固件上电
   ↓
连 WiFi（NVS 存的 SSID/密码，失败则开热点配网）
   ↓
连 ws://<pc>:3082/dev?token=...
   ↓ 成功
发 hello  ──────────────→  桥接记录设备
   ↓                        ↓
   ←────────────────────  推 state（立即一次，之后 1Hz 或变化时）
   ↓
每 10s 发 ping  ────────→  回 pong
   ↓
收到 ask  →  黄闪+响铃+显示按钮
   ↓
用户点按钮  →  发 answer  →  桥接调 DSH 的 respond
   ↓
断线  →  1s/2s/4s/8s… 指数退避重连（上限 30s），屏幕显示 offline
```

### 2.6 诚实状态原则

> 借鉴 `angaziz/beacon`（MIT）的设计原则：**a value is shown as loading / live / stale / offline — never a guess dressed up as live data.**

固件必须区分四种数据新鲜度，不能把过期数据当实时显示：

| 状态 | 判定 | 表现 |
|------|------|------|
| `loading` | 已连接但还没收到第一帧 state | 灰色骨架 |
| `live` | 最后一帧 state 在 3s 内 | 正常颜色 |
| `stale` | 最后一帧 state 超过 3s | 数值变灰 + 小时钟图标 |
| `offline` | WebSocket 断开 | 暗屏 + 断线图标，**不显示任何数值** |

这条原则很重要：一个显示"任务进行中"但其实早就断线的仪表盘，比一个明确显示"离线"的仪表盘更糟 —— 后者你知道要去看电脑，前者会让你放心地不去看。

---

## 第三部分：固件复用来源与许可

| 组件 | 来源 | 许可 | 动作 |
|------|------|------|------|
| ST77916 屏驱动 | `Codex-Macro32/firmware/Codex-Micro-1.0/src/esp_lcd_st77916.c` | Apache-2.0（Waveshare/Espressif 上游） | ✅ 复用，保留版权头 |
| CST816 触摸 | 同上 `Touch_CST816.cpp` | Apache-2.0 / MIT | ✅ 复用 |
| I2C 驱动 | 同上 `I2C_Driver.cpp` | MIT (imliubo) | ✅ 复用 |
| LVGL port | 同上 `LvglPort.cpp` | MIT (imliubo) | ✅ 复用 |
| 音频反馈 | 同上 `AudioFeedback.cpp` | MIT (imliubo) | ✅ 复用 |
| `platformio.ini` | 同上（已验证的 pioarduino 平台配置） | — | ✅ 复用（配置不构成实质创作） |
| BLE 传输层 | 同上 `CodexMicroBle.cpp` | MIT | ❌ 不用，换成 WiFi/WebSocket |
| Codex UI 视觉表达 | 同上 `CodexUi.cpp` | **有限使用许可** | ❌ 不用，自己设计圆屏布局 |
| 权限队列交互设计 | `angaziz/beacon` docs | MIT | ✅ 借鉴思路 |
| 主机桥（Python/BLE/macOS） | `Codex-Macro32/companion/` | 有限使用许可 | ❌ 不用，重写为 Node |

**许可依据**：`Codex-Macro32/LIMITED_SOURCE_USE.md` 明文声明"不能把本说明解释为撤销、替代或限制第三方 MIT、Apache-2.0 或其他许可证已经授予的权利"，且 `firmware/Codex-Micro-1.0/LICENSE` 是 `MIT (c) 2026 imliubo`，`CodexMicroBle.h` 头部带 `SPDX-License-Identifier: MIT`。因此驱动层可自由复用；作者新增的品牌与 UI 视觉表达受限，我们自己写。

---

## 第四部分：实现状态

### 已完成并验证

| 项 | 状态 |
|---|---|
| DSH RPC wire 格式 | ✅ 实测破解，`session.list` 稳定可用 |
| 事件流四种帧结构 | ✅ 实测记录（见 1.5） |
| 桥接进程 | ✅ 14/14 自测通过（真实 DSH 数据） |
| token 认证 | ✅ 错误 token 返回 401，恒定时间比较 |
| `state` / `ask` / `answer` / `ping` / `pong` / `beep` 全链路 | ✅ 假设备走真实 WebSocket 验证 |
| 固件代码 | ✅ 已写完（WsClient / DialUi / main） |
| 板级驱动 | ✅ 17 个文件已 vendored（MIT/Apache，出处可查） |

### 待真机验证

| 项 | 需要什么 |
|---|---|
| 固件编译 | 装 PlatformIO（`python -m pip install platformio`） |
| 屏幕渲染 | 烧录后目视 |
| 触摸应答 | 触发一次真实审批 |
| 审批帧结构 | 第一次真实审批时看桥接日志的 `ASK-SHAPED FRAME` 行 |
| 提示音 | 板上是 PCM5101，vendored 音频按 ES8311 写 —— 预期静默降级 |

### 已知取舍

1. **`session/event` 不转发** —— 每 token 一帧，圆屏无处渲染，会淹没链路。仪表盘用 `session/jobs` 的粗粒度状态。
2. **`cmd` 动作是空壳** —— 本版 DSH 没有 `jobs.cancel` / `session.cancel` 端点，固件侧路径已通，主机侧待端点出现后接上。
3. **电量是占位值** —— 板上有充电管理但 I2C 上没有电量计，需要接电池分压后读 ADC。
4. **音频按 ES8311 写** —— vendored 代码来自姊妹板；本板 PCM5101 会让 `begin()` 返回 false，固件静默运行，功能不受影响。

---

## 第二部分 增补：语音输入（Push-to-Talk）—— 设计定稿

> 状态：**已定稿，待实现**。硬件约束已核实（见 §5.2），协议字段已与固件/桥接
> 现有代码对齐。UI 表现层留待用户确认后另行调整，本部分只定义行为与协议。

### 5.1 交互模型（对讲机式）

```
按下 BOOT 键 ──► 圆盘进入录音态（屏幕显示波形/倒计时）
     │
     ├─ 按住不放 ──► I2S 麦克风持续采样，二进制帧实时上行
     │
     └─ 松开 ──► 发 voice_end → 桥接拼接完整音频 → ASR → 文本回填
```

| 输入方式 | 触发 | 何时使用 |
|---|---|---|
| **语音（主）** | 按住 BOOT 说话，松开发送 | DSH 提问需要文字/任意答案 |
| **触摸选项（辅）** | 屏幕按钮 | 预置选项（允许/拒绝/三选一等） |
| 键盘 | ❌ 已否决 | 用户确认：实体键 + 语音 > 屏上打字 |

**为什么是 BOOT 键**：板上仅有两个实体键（BOOT=GPIO0、RESET）。RESET 复用会引入
"长按重启 vs 长按录音"的歧义；BOOT 在正常运行时不承担任何职责，按住即录、
松开即发，语义零冲突。GPIO0 默认上拉（内部 pullup），按下接低 —— 用
`digitalRead == LOW` 判断按下，软件去抖 30ms。

### 5.2 硬件事实（已核实）

| 项 | 值 | 来源 |
|---|---|---|
| 实体按键 | BOOT=GPIO0（默认拉高），RESET 不用 | Waveshare 1.85B 原理图 |
| 麦克风 | I2S 数字麦克风，`kDinPin=39`（I2S 数据输入） | vendor/AudioFeedback.cpp |
| I2S 其他引脚 | BCLK=48, LRCLK=38, DOUT=47, MCLK=2 | 同上 |
| 采样配置 | 16 kHz · 16-bit · 单声道（语音识别够用，省带宽） | 本文定稿 |
| 板载 DAC | PCM5101（只出不出进），与麦克风输入互不冲突 | 项目文档 |

**I2S 总线复用**：AudioFeedback 用 `I2S_MODE_STD` 播放提示音。录音用同一总线的
输入方向（ESP32-S3 的 I2S 是半双工）。**同一时刻只允许一个方向活跃**：播放
提示音前若在录音，先停录音。这是实现里必须强制的不变量。

### 5.3 上行协议（固件 → 桥接）

帧类型沿用现有 wire：JSON 文本帧 = 控制，二进制帧 = 音频（新）。

```
[控制帧] {"t":"voice_start","askId":"ask-xxx"}          ← 按下 BOOT，若有活动 ask 带上 id
[二进制帧] 原始 PCM16LE 采样，每帧 ≤ 1024 字节            ← 按住期间持续发送
[控制帧] {"t":"voice_end","samples":N,"durationMs":D}    ← 松开 BOOT
```

- 音频格式：**PCM s16le mono 16 kHz**，无 WAV 头（桥接负责加头）。
- 最小片段：`durationMs < 400ms` 判定为空录音，桥接丢弃并提示"没听清"。
- 上限：`durationMs > 30s` 由桥接截断，防止误按后整段垃圾上传。
- 发送节奏：主循环每轮尽量发完缓冲（I2S DMA 缓冲 ~32ms/次），不阻塞 UI。

### 5.4 下行协议（桥接 → 固件）

```
{"t":"voice_result","text":"识别出的文字","partial":false}
{"t":"voice_error","message":"识别服务不可用"}
```

- 有活动 ask 时：`voice_result` 直接作为该 ask 的答案提交（复用 `respond`），
  圆盘显示"识别结果：xxx"。
- 无 ask（用户主动语音命令）时：桥接把文本发给 DSH 的事件流（预留 `voice command` 通道）。
- 识别期间圆盘显示"识别中…"，完成/失败各有一个明确反馈。

### 5.5 ASR 方案（桥接侧，按优先级）

| 方案 | 方式 | 延迟 | 成本 | 备注 |
|---|---|---|---|---|
| **① 云端 API（首选落地）** | 音频帧收齐 → 拼 WAV → POST 到云端 ASR | ~1-2s | 按量计费 | 中文识别质量最好 |
| ② Windows 本地语音 | `System.Speech` / PowerShell 调本地识别 | ~0.5s | 免费 | 公司机可用，质量一般 |
| ③ Whisper 本地模型 | `faster-whisper` 等 | 1-3s | 免费 | 需要装 Python 环境 |
| ④ 直接把音频丢给 DSH | 作为附件上传，智能体自己听 | 秒级 | 免费 | 文档已评估，作 fallback |

**定稿选择**：默认走 **①云端 API**（配置里给 `asr.provider`，可在 console 切换），
识别失败自动降级到 **④ attach 音频给 DSH**。②、③ 作为可选本地项。

### 5.6 UI 表现（待用户确认，先记录意向）

- 录音态：**圆盘中央大圆按钮 → 红色脉冲**，"正在录音 · 松开发送"。
- 识别态：**转圈/加载动画**，"正在识别…"。
- 结果态：**绿色 ✓ + 识别文本**，1.5s 后回到主界面。
- 失败态：**红色 ✕ + 提示**"没听清，再试一次"。
- 空闲唤醒：短按 BOOT（<400ms）进入"语音命令"模式，可对 DSH 说话而不必有 ask。

### 5.7 固件实现拆分

| 模块 | 文件 | 内容 |
|---|---|---|
| 二进制帧 | `WsClient.h/.cpp` | `sendBinary()`（FIN+二进制 opcode 0x2，带掩码） |
| 按键 | `VoiceInput.h/.cpp`（新） | GPIO0 去抖、长按/短按判定、状态机 |
| 录音 | 同上 | I2S 读取 16kHz/16bit/mono → 环形缓冲 → 分帧上行 |
| 生命周期 | `main.cpp` | 在 `serviceRunning()` 中驱动录音状态机 |
| UI 表现 | `DialUi.h/.cpp` | 新增录音/识别/结果 overlay（与 ask overlay 同级） |
| 桥接 | `bridge.js` | 帧读取器支持二进制；`voice_start/end` 处理；ASR 适配层 |

### 5.8 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| 采样率不匹配 ASR 要求 | 中 | 桥接端做 16k→任意重采样，固件固定 16k 输出 |
| I2S 播放/录音冲突 | 中 | 不变量：播放前必须停录音（§5.2） |
| 长按录音时误触屏幕 | 低 | 录音态抬高 BOOT 优先，触摸事件忽略 |
| 录音损坏/爆音 | 低 | 环形缓冲丢尾不丢头；上线后串口日志校验 |
| 中文 ASR 不准 | 中 | provider 可切换；结果回显让用户确认后再提交 |
